static_library("learning") {
    sources = [
        "arow.cc",
        "arow.h",
        "multi_layer_perceptron.cc",
        "multi_layer_perceptron.h",
    ]

    deps = [
        "//third_party/glog",
    ]
}
