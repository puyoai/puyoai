config("compiler") {
  cflags = []
  ldflags = []

  if (is_posix) {
    cflags += [
      "-pthread",
      "-march=native",
    ]
    ldflags += [
      "-pthread",
      "-march=native",
    ]
  }
}

# optimization

config("optimize") {
  if (is_posix) {
    cflags = [ "-O2" ]
  }
}

config("no_optimize") {
  if (is_posix) {
    cflags = [ "-O0" ]
  }
}
