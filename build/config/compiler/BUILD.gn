config("compiler") {
  cflags = []
  ldflags = []

  if (os == "linux") {
    cflags += [
      "-pthread",
    ]
    ldflags += [
      "-pthread",
    ]
  }
}

# optimization

config("optimize") {
  if (os == "linux") {
    cflags = [ "-O2" ]
  }
}

config("no_optimize") {
  if (os == "linux") {
    cflags = [ "-O0" ]
  }
}
